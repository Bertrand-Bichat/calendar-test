<p id="notice"><%= notice %></p>

<p>
  <strong>User:</strong>
  <%= @restaurant.user %>
</p>

<p>
  <strong>Name:</strong>
  <%= @restaurant.name %>
</p>

<p>
  <strong>Content:</strong>
  <%= @restaurant.content %>
</p>

<%= link_to 'Edit', edit_restaurant_path(@restaurant) %> |
<%= link_to 'Back', restaurants_path %>

<div class="container">
  <%= week_calendar(number_of_days: @rental.number_of_day) do |date| %>
    <%= date.strftime("%d/%m/%Y") %>
  <% end %>
</div>
